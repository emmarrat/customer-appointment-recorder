import { borderRadius, boxShadow } from '../../../../stylesMui';

export const styles = {
  card: {
    width: '320px',
    borderRadius,
    boxShadow,
  },
  media: {
    height: '150px',
  },
  wrapper: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    borderRadius,
  },
};
